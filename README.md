# 🎬 Watchlist API (Express + Prisma)

یک API ساده، تمیز و قابل توسعه برای مدیریت **لیست تماشا و فیلم‌های مورد علاقه** کاربران.  
این پروژه با **Express.js** نوشته شده و از **Prisma ORM** به همراه دیتابیس **NeonDB (PostgreSQL)** استفاده می‌کند.

دارای سیستم **احراز هویت کامل با JWT** است و هر کاربر فقط به لیست شخصی خودش دسترسی دارد.

## ✨ امکانات پروژه

- 🔐 **احراز هویت (Authentication & Authorization)**
  - ثبت‌نام و ورود کاربران
  - صدور و اعتبارسنجی JWT
  - Middleware محافظت از روت‌های خصوصی (`authMiddleware`)
- 🎥 **مدیریت Watchlist (CRUD کامل)**
  - افزودن فیلم/سریال به لیست تماشا
  - ویرایش اطلاعات آیتم (وضعیت تماشا، امتیاز شخصی، یادداشت و ...)
  - حذف آیتم از لیست
  - دریافت تمام آیتم‌های یک کاربر با قابلیت فیلتر و صفحه‌بندی
- 🗄️ **Prisma ORM + NeonDB**
  - مدل‌های تمیز و تایپ‌سیف
  - مهاجرت‌های دیتابیس با Prisma Migrate
  - اتصال امن به NeonDB (PostgreSQL serverless)
- 🚀 **Express 5 + ساختار ماژولار**
  - کد تمیز و قابل نگهداری
  - جداسازی کامل route‌ها، controller‌ها، service‌ها و middleware‌ها
  - آماده scale کردن به پروژه‌های بزرگ‌تر

## 📦 تکنولوژی‌ها و ابزارها

| بخش              | تکنولوژی                  |
|------------------|---------------------------|
| زبان             | Node.js (ESM)             |
| فریم‌ورک         | Express.js 5              |
| ORM              | Prisma                    |
| دیتابیس          | NeonDB (PostgreSQL)       |
| احراز هویت       | JWT + bcryptjs            |
| اعتبارسنجی       | Zod یا express-validator   |
| محیط توسعه       | Nodemon + Prisma Studio   |
| فرمت کد          | ESLint + Prettier         |

## 📁 ساختار پروژه



├── src/
│   ├── config/          # تنظیمات (DB، JWT، env)
│   ├── controllers/     # منطق روت‌ها
│   ├── middleware/      # auth, error handler, ...
│   ├── routes/          # تعریف روت‌ها
│   ├── services/        # منطق بیزینسی
│   ├── utils/           # توابع کمکی
│   ├── prisma/          # schema.prisma و migrations
│   └── server.js        # نقطه ورود برنامه
├── .env.example
├── prisma/
│   └── schema.prisma
├── package.json
└── README.md


## ⚙️ راه‌اندازی سریع

```bash
# 1. کلون کردن پروژه
git clone https://github.com/username/watchlist-api.git
cd watchlist-api

# 2. نصب پکیج‌ها
npm install

# 3. کپی و تنظیم متغیرهای محیطی
cp .env.example .env
# داخل .env مقدارهای زیر را تنظیم کنید:
# DATABASE_URL="postgresql://..."
# JWT_SECRET="your_very_strong_secret"
# JWT_EXPIRES_IN="7d"

# 4. اجرای مهاجرت‌های دیتابیس
npx prisma migrate dev

# 5. اجرای پروژه
npm run dev

```
متد,مسیر,توضیح,دسترسی
POST,/api/auth/register,ثبت‌نام,عمومی
POST,/api/auth/login,ورود و دریافت توکن,عمومی
GET,/api/watchlist,دریافت لیست تماشای کاربر,خصوصی
POST,/api/watchlist,افزودن آیتم جدید,خصوصی
PUT,/api/watchlist/:id,ویرایش آیتم,خصوصی
DELETE,/api/watchlist/:id,حذف آیتم,خصوصی

🔒 امنیت

رمز عبور با bcrypt هش می‌شود
توکن JWT در کوکی httpOnly (یا هدر) ارسال می‌شود
تمام روت‌های خصوصی با middleware احراز هویت محافظت شده‌اند

Made with ❤️ by [m-maroufi]
